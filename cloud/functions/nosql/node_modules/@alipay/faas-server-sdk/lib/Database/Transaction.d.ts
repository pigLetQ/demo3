import type { Httpclient } from '@alipay/faas-common-sdk';
import { DatabaseCloudConfig } from '../types';
import { Collection } from './Collection';
export declare class Transaction {
    #private;
    static COMMIT_FLAG: number;
    static ROLLBACK_FLAG: number;
    reason: any;
    private constructor();
    commit(): Promise<void>;
    rollback(reason: any): Promise<void>;
    collection(collectionName: string): Collection;
    isProcessing(): boolean;
    isRollback(): boolean;
    isCommitted(): boolean;
    static create(config: DatabaseCloudConfig, httpclient: Httpclient): Promise<Transaction>;
}
