import { FaasError } from '@alipay/faas-common-sdk';
export declare const errors: {
    INVALID_PARAM(paramName: string, message: string): FaasError;
    NETWORK_ERR(e: Error, requestID: string): FaasError;
    STORAGE_DOWNLOAD_FILE_ERR(status: number, requestID: string): FaasError;
    STORAGE_UPLOAD_FILE_ERR(status: number, requestID: string): FaasError;
    INVALID_OPENAPI_RESPONSE_ERR(requestId: string, res: any): FaasError;
    NOT_FOUND_ERR(message: string): FaasError;
};
