import type { Next } from 'koa';
import type { HttpRequestContext } from '../HttpRequestContext';
export declare function createInitCallDataProxyHeadersMiddleware(dataApiType: string): <T, R>(ctx: HttpRequestContext<T, R>, next: Next) => Promise<void>;
