import { BaseOpenapiOptions, BaseOpenapiResult } from './common';
export interface AlipayOpenMiniVersionBaseauditPassedOptions extends BaseOpenapiOptions {
    bizContent: {
        audit_reason: string;
        base_audit: string;
        base_audit_reason?: {
            audit_images?: string[];
            memos?: {
                memo?: string;
                memo_image_list?: string[];
            }[];
        };
        bundle_id?: string;
        mini_app_id: string;
        mini_app_version: string;
        promote_audit: string;
        promote_audit_reason?: {
            audit_images?: string[];
            memos?: {
                memo?: string;
                memo_image_list?: string[];
            }[];
        };
    };
}
export interface AlipayOpenMiniVersionBaseauditPassed {
    request(options: AlipayOpenMiniVersionBaseauditPassedOptions): Promise<BaseOpenapiResult>;
}
