import { QueryCommand } from '../AbstractCommand';
export declare class NotCommand extends QueryCommand {
    private readonly command;
    constructor(command: object | QueryCommand);
    stringify(): any;
}
