import { QueryCommand } from '../AbstractCommand';
export declare class AndCommand extends QueryCommand {
    private readonly commands;
    constructor(commands: Array<object | QueryCommand>);
    pushCommand(cmd: QueryCommand): void;
    stringify(): any;
}
