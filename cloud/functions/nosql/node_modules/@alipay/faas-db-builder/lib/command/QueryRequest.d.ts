import { QueryChainCommand } from '../typing';
export declare class QueryRequest {
    private readonly query;
    private readonly prefix;
    constructor(query: QueryChainCommand, prefix?: string);
    stringify(): any;
    toJSON(): any;
}
