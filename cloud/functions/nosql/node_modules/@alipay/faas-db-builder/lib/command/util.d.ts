import { QueryChainCommand, UnchainCommand } from '../typing';
export declare class CommandUtils {
    static stringify(cmd: QueryChainCommand): any;
    static unchain<T = QueryChainCommand>(cmd: T): UnchainCommand<T>;
}
