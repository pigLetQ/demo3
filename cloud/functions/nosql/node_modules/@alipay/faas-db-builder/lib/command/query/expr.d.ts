import { AggregateCommand, QueryCommand } from '../AbstractCommand';
export declare class ExprCommand extends QueryCommand {
    private readonly commands;
    constructor(cmd: AggregateCommand);
    stringify(): any;
}
