import { RegExpParam } from '../typing';
export declare class Regex {
    private readonly regexp;
    private readonly options;
    constructor(expression: RegExpParam);
    stringify(): {
        $regex: string;
        $options: string;
    };
}
