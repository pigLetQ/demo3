import { UpdateCommand } from '../AbstractCommand';
export declare class MulCommand extends UpdateCommand {
    private readonly num;
    constructor(num: number);
    get operator(): string;
    stringify(): number;
}
