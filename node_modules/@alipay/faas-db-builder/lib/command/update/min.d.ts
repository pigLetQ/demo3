import { UpdateCommand } from '../AbstractCommand';
export declare class MinCommand extends UpdateCommand {
    private readonly num;
    constructor(num: any);
    get operator(): string;
    stringify(): any;
}
