import { UpdateArrayCommand } from '../AbstractCommand';
export declare class PullAllCommand extends UpdateArrayCommand {
    private readonly values;
    constructor(values: any[]);
    get operator(): string;
    stringify(): any[];
}
