import { QueryCommand } from '../AbstractCommand';
export declare class ModCommand extends QueryCommand {
    private readonly divisor;
    private readonly remainder;
    constructor(divisor: number, remainder: number);
    stringify(): any;
}
