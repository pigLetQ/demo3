import { MongoCollectionListQueryOperation, MongoCollectionOperation, MongoDocumentOperation } from '../typing';
import { CollectionAction, DocumentAction } from './constant';
export declare class ActionBuilder {
    static buildCollectionAction(action: CollectionAction, collectionName: string): MongoCollectionOperation;
    static buildCollectionListQueryAction(options: Record<string, unknown>): MongoCollectionListQueryOperation;
    static buildDocumentAction(action: DocumentAction, collectionName: string, options: Record<string, unknown>): MongoDocumentOperation;
}
