export declare class Operator {
    private readonly res;
    constructor();
    set(key: string, val: any): void;
    applyOperator(operator: string, key: string, val: any): void;
    mergeOperator(operator: string, value: object): void;
    emit(): Record<string, any>;
}
