import { UpdateCommand } from '../AbstractCommand';
export declare class IncCommand extends UpdateCommand {
    private readonly num;
    constructor(num: number);
    get operator(): string;
    stringify(): number;
}
