import { UpdateCommand } from '../AbstractCommand';
export declare class SetCommand extends UpdateCommand {
    private readonly data;
    constructor(data: object);
    get operator(): string;
    stringify(): object;
}
