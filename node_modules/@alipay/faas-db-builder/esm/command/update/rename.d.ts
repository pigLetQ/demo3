import { UpdateCommand } from '../AbstractCommand';
export declare class RenameCommand extends UpdateCommand {
    private readonly name;
    constructor(name: string);
    get operator(): string;
    stringify(): string;
}
