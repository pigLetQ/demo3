import { UpdateCommand } from '../AbstractCommand';
export declare class MaxCommand extends UpdateCommand {
    private readonly num;
    constructor(num: any);
    get operator(): string;
    stringify(): any;
}
