import { QueryCommand } from '../AbstractCommand';
import { QueryChain } from './QueryChain';
export declare class ElemMatchCommand extends QueryCommand {
    private readonly val;
    constructor(val: object | QueryCommand | QueryChain);
    stringify(): any;
}
